# -*- coding: utf-8 -*-
from odoo.tests.common import TransactionCase

class TestMyModule(TransactionCase):
    def test_basic(self):
        # A simple test to verify the module loads and runs
        self.assertTrue(True)
